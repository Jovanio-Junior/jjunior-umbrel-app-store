version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: jovanio-tldraw_web_1
      APP_PORT: 80

  web:
    image: jumbrelappson/tldraw:5.1.2
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
