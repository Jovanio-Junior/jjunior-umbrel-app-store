version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: jovanio-tldraw_web_1
      APP_PORT: 80

  web:
    image: jumbrelappson/tldraw:5.1.1@sha256:86f35fb3c8d646e73377cb0a9b9b6f2d1a2344aef86c329d1ef27de5626e4a71
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
